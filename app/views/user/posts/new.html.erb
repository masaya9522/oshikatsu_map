<h1>投稿</h1>
<%= form_with model: Post.new do |f| %>

  <h4>画像</h4>
  <%= f.file_field :image, accept: "image/*" %>

  <h4>説明</h4>
  <%= f.text_area :introduction %>

  <h4>タグ</h4>
  <%= f.text_field :tag %>

  <%= f.submit '投稿' %>
<% end %>